# GitLab CI/CD

This document defines a self-managed GitLab workflow for the GX10 deployment.

The pipeline validates configuration, builds Arm64-compatible artifacts, and runs protected deployment or GPU smoke-test jobs on the GX10.
