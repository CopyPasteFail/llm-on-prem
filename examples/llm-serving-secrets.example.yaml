# Example only. Do not commit a populated Secret to Git.
# Create the real Secret through the approved secret-management process.
apiVersion: v1
kind: Secret
metadata:
  name: llm-serving-secrets
  namespace: llm-serving
type: Opaque
stringData:
  LITELLM_MASTER_KEY: REPLACE_WITH_A_LONG_RANDOM_VALUE
  VLLM_API_KEY: REPLACE_WITH_A_DIFFERENT_LONG_RANDOM_VALUE
  # DATABASE_URL: postgresql://...  # Required only when LiteLLM database-backed features are enabled.
